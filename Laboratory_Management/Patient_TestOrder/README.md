# Patient Service

## Chạy ứng dụng
1. Cài PostgreSQL và tạo database `healthcare_db`
2. Sửa thông tin database trong `application.properties`
3. Chạy: `mvn spring-boot:run`

## API
- `/api/patients` - Patient endpoints
- `/api/patient-medical-records` - Medical record endpoints
